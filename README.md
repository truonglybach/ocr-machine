# ocr-machine
A Python tool that batch-processes scanned PDFs with Tesseract OCR, identifies pages where text recognition failed, and searches across processed files for specific strings. Generates CSV reports for both OCR results and search results.
